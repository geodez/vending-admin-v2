# 🧹 Подготовка сервера для нового проекта

**Перед деплоем vending-admin-v2**

---

## ⚠️ Важно: Резервное копирование

Перед удалением старого проекта создайте бэкап БД!

```bash
# 1. Подключитесь к серверу
ssh your-server

# 2. Бэкап старой базы данных (если есть данные)
cd /opt/vending-git/backend
docker-compose exec db pg_dump -U vending -d vending > /tmp/vending_backup_$(date +%Y%m%d_%H%M%S).sql

# Или если база на хосте:
pg_dump -U vending -d vending > /tmp/vending_backup_$(date +%Y%m%d_%H%M%S).sql

# 3. Скопировать бэкап в безопасное место
sudo cp /tmp/vending_backup_*.sql ~/backups/
```

---

## 🛑 Остановка и удаление старого проекта

### Вариант 1: Полная очистка (если старый проект больше не нужен)

```bash
# 1. Перейти в директорию старого проекта
cd /opt/vending-git/backend

# 2. Остановить и удалить контейнеры
docker-compose down

# 3. Удалить volumes (ВНИМАНИЕ: удалятся все данные!)
docker-compose down -v

# 4. Проверить что контейнеры удалены
docker ps -a | grep vending

# 5. Удалить директорию проекта
cd /opt
sudo rm -rf vending-git

# 6. Проверить освобожденное место
df -h
```

### Вариант 2: Сохранить старый проект (переименовать)

```bash
# 1. Остановить старый проект
cd /opt/vending-git/backend
docker-compose down

# 2. Переименовать директорию
cd /opt
sudo mv vending-git vending-git-old

# 3. Старый проект сохранен в /opt/vending-git-old
# При необходимости можно запустить заново:
# cd /opt/vending-git-old/backend && docker-compose up -d
```

---

## 🔍 Проверка портов

Убедитесь что порты свободны:

```bash
# Проверить порт 8000 (FastAPI)
sudo lsof -i :8000

# Проверить порт 5432 (PostgreSQL)
sudo lsof -i :5432

# Если порты заняты — остановить процесс:
sudo kill $(sudo lsof -t -i :8000)
sudo kill $(sudo lsof -t -i :5432)
```

---

## 🐳 Очистка Docker

### Удалить неиспользуемые образы и volumes

```bash
# Удалить остановленные контейнеры
docker container prune -f

# Удалить неиспользуемые образы
docker image prune -a -f

# Удалить неиспользуемые volumes
docker volume prune -f

# Полная очистка (осторожно!)
docker system prune -a --volumes -f

# Проверить место на диске
df -h
docker system df
```

---

## 📦 Проверка зависимостей

### Docker и Docker Compose

```bash
# Проверить Docker
docker --version

# Проверить Docker Compose
docker-compose --version
# или
docker compose version

# Если не установлены — установить:
# Ubuntu/Debian
sudo apt update
sudo apt install docker.io docker-compose -y
sudo systemctl start docker
sudo systemctl enable docker

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
# Выйти и зайти заново для применения
```

### Git

```bash
# Проверить Git
git --version

# Если не установлен:
sudo apt install git -y
```

---

## 🗂️ Подготовка директорий

```bash
# 1. Создать директорию для нового проекта
sudo mkdir -p /opt/vending-admin-v2
sudo chown $USER:$USER /opt/vending-admin-v2

# 2. Создать директорию для логов
sudo mkdir -p /var/log/vending
sudo chown $USER:$USER /var/log/vending

# 3. Создать директорию для бэкапов
mkdir -p ~/backups/vending
```

---

## 🔥 Очистка Nginx конфигурации (если использовался)

### Если старый проект был настроен с Nginx

```bash
# 1. Удалить старые конфигурации
sudo rm /etc/nginx/sites-enabled/vending-admin
sudo rm /etc/nginx/sites-available/vending-admin

# 2. Проверить конфигурацию
sudo nginx -t

# 3. Перезапустить Nginx
sudo systemctl reload nginx
```

---

## 🔐 Очистка SSL сертификатов (если использовались)

### Если были настроены SSL сертификаты для старого домена

```bash
# Список сертификатов
sudo certbot certificates

# Удалить старый сертификат (если нужен новый домен)
sudo certbot delete --cert-name api.old-domain.com

# Или оставить, если домен тот же
```

---

## 🧪 Проверка окружения

### Чек-лист перед деплоем

```bash
# 1. Docker работает
docker ps

# 2. Порты свободны
sudo lsof -i :8000
sudo lsof -i :5432

# 3. Достаточно места на диске (минимум 5GB)
df -h

# 4. Git установлен
git --version

# 5. Директория /opt свободна или подготовлена
ls -la /opt

# 6. Интернет-соединение работает
ping -c 3 github.com
```

---

## 📊 Миграция данных из старого проекта (опционально)

### Если нужно перенести данные из старой БД в новую

**1. Создать бэкап старой БД:**
```bash
cd /opt/vending-git/backend
docker-compose up -d db
docker-compose exec db pg_dump -U vending -d vending > /tmp/old_vending.sql
```

**2. После деплоя новой версии:**
```bash
cd /opt/vending-admin-v2/backend

# Применить миграции новой версии
docker-compose exec app alembic upgrade head

# Импортировать ТОЛЬКО нужные таблицы (осторожно!)
# Например, только транзакции:
docker-compose exec db psql -U vending -d vending -c "
COPY vendista_tx_raw FROM '/tmp/old_vendista_tx_raw.csv' CSV HEADER;
"
```

**⚠️ Внимание:**
- Новая версия имеет другую структуру БД (User model изменен)
- Прямой импорт старого дампа может не работать
- Лучше начать с чистой БД и настроить заново

---

## ✅ Итоговый чек-лист подготовки

- [ ] Создан бэкап старой БД (если есть важные данные)
- [ ] Старый проект остановлен (`docker-compose down`)
- [ ] Старая директория удалена или переименована
- [ ] Docker и Docker Compose работают
- [ ] Порты 8000 и 5432 свободны
- [ ] Достаточно места на диске (минимум 5GB)
- [ ] Git установлен
- [ ] Директория `/opt` готова для нового проекта
- [ ] Nginx конфиги очищены (если были)
- [ ] SSL сертификаты проверены (если были)

---

## 🚀 Следующий шаг

После подготовки сервера переходите к деплою:

```bash
# Клонировать новый проект
cd /opt
git clone https://github.com/geodez/vending-admin-v2.git
cd vending-admin-v2/backend

# Следовать инструкции из QUICK_DEPLOY.md
```

---

## 🆘 Решение проблем

### Docker не запускается

```bash
sudo systemctl status docker
sudo systemctl start docker
sudo journalctl -u docker -n 50
```

### Порт занят

```bash
# Найти процесс
sudo lsof -i :8000

# Убить процесс
sudo kill -9 <PID>

# Или остановить старый docker-compose
cd /opt/vending-git/backend
docker-compose down
```

### Недостаточно места

```bash
# Проверить место
df -h

# Очистить Docker
docker system prune -a --volumes -f

# Очистить системный кэш
sudo apt clean
sudo apt autoclean
sudo apt autoremove
```

### Проблемы с правами

```bash
# Добавить пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиниться
exit
ssh your-server

# Проверить
docker ps
```

---

## 📋 Рекомендуемая последовательность действий

### Минимальная очистка (быстро):
1. `cd /opt/vending-git/backend && docker-compose down`
2. `cd /opt && sudo rm -rf vending-git`
3. Готово! Переходите к деплою нового проекта

### Полная очистка (рекомендуется):
1. Создать бэкап БД
2. Остановить старый проект
3. Удалить старую директорию
4. Очистить Docker (`docker system prune -a --volumes -f`)
5. Проверить порты
6. Проверить место на диске
7. Готово! Переходите к деплою нового проекта

---

**После подготовки сервера переходите к:** [QUICK_DEPLOY.md](./QUICK_DEPLOY.md)
