# 📋 План необходимых доработок

**Дата создания:** 2026-01-24  
**Версия проекта:** v1.1.1  
**Статус:** После рефакторинга

---

## 🎯 Цель плана

Улучшить проект, реализовав недостающие функции и исправив несоответствия между документацией и реализацией.

---

## 📊 Приоритеты доработок

### 🔴 Высокий приоритет (критично для функциональности)

#### 1. Batch операции для кнопок терминалов

**Проблема:** В документации указаны batch операции, но они не реализованы как отдельные эндпоинты.

**Текущее состояние:**
- ✅ Есть система Button Matrix (шаблоны)
- ✅ Есть массовая привязка терминалов к матрице
- ❌ Нет отдельного эндпоинта `POST /api/v1/buttons/batch-map`
- ❌ Нет операции клонирования `POST /api/v1/buttons/clone`

**Что нужно реализовать:**

1. **Batch-map для Button Matrix Items**
   ```python
   POST /api/v1/mapping/button-matrices/{matrix_id}/items/batch
   ```
   - Массовое добавление/обновление элементов матрицы
   - Принимает список: `[{machine_item_id, drink_id, sale_price_rub}, ...]`
   - Валидация всех элементов перед применением
   - Транзакционная обработка (все или ничего)

2. **Clone матрицы между локациями**
   ```python
   POST /api/v1/mapping/button-matrices/{matrix_id}/clone
   ```
   - Копирование матрицы с элементами
   - Привязка к другой локации/терминалам
   - Опция переименования матрицы

**Оценка:** 4-6 часов

---

#### 2. Дополнительные эндпоинты аналитики

**Проблема:** Некоторые эндпоинты из документации не реализованы.

**Что нужно реализовать:**

1. **GET /api/v1/analytics/sales/summary**
   - Сводка продаж (общая статистика)
   - Агрегированные метрики за период

2. **GET /api/v1/analytics/sales/margin**
   - Детальная маржинальность по продуктам
   - Сравнение маржи между периодами

3. **GET /api/v1/analytics/owner-report/daily**
   - Детальный отчет по дням (расширение текущего `/owner-report`)

4. **GET /api/v1/analytics/owner-report/issues**
   - Список проблем и алертов
   - Критические остатки, низкая маржа, ошибки синхронизации

**Оценка:** 6-8 часов

---

#### 3. Эндпоинты для расходов

**Проблема:** Не реализованы аналитические эндпоинты для расходов.

**Что нужно реализовать:**

1. **GET /api/v1/expenses/analytics**
   - График расходов по дням
   - Сравнение между категориями
   - Тренды расходов

2. **GET /api/v1/expenses/categories**
   - Справочник категорий расходов
   - Статистика по категориям

**Оценка:** 3-4 часа

---

### 🟡 Средний приоритет (улучшение UX)

#### 4. Клонирование рецептов

**Проблема:** В документации указано клонирование рецептов, но не реализовано.

**Что нужно реализовать:**

```python
POST /api/v1/business/drinks/{drink_id}/clone
```

- Копирование рецепта с ингредиентами
- Возможность изменения названия
- Валидация ингредиентов в целевой локации

**Оценка:** 2-3 часа

---

#### 5. Улучшение системы алертов

**Проблема:** Алерты упоминаются, но нет отдельного сервиса.

**Что нужно реализовать:**

1. **Создать `services/alert_service.py`**
   - Генерация алертов по остаткам
   - Алерты по низкой марже
   - Алерты по ошибкам синхронизации

2. **Эндпоинт для алертов**
   ```python
   GET /api/v1/analytics/alerts
   ```
   - Список активных алертов
   - Фильтрация по типу и критичности

**Оценка:** 4-5 часов

---

#### 6. Выделение KPI Calculator

**Проблема:** Логика расчета KPI разбросана по `analytics.py`.

**Что нужно реализовать:**

1. **Создать `services/kpi_calculator.py`**
   - Расчет всех KPI метрик
   - Кэширование результатов
   - Оптимизация запросов

2. **Рефакторинг `analytics.py`**
   - Использование KPI Calculator
   - Упрощение кода эндпоинтов

**Оценка:** 3-4 часа

---

### 🟢 Низкий приоритет (опционально)

#### 7. Расширенная система RBAC

**Проблема:** RBAC упрощен до ролей, нет таблиц permissions.

**Что нужно реализовать (опционально):**

1. Создать таблицы `permissions` и `role_permissions`
2. Реализовать `auth/permissions.py`
3. Миграция существующих пользователей
4. Обновить проверки прав доступа

**Оценка:** 8-10 часов  
**Примечание:** Текущая система работает, расширение не критично

---

#### 8. Документация API

**Проблема:** Swagger документация может быть неполной.

**Что нужно сделать:**

1. Добавить детальные описания всех эндпоинтов
2. Примеры запросов/ответов
3. Описание ошибок

**Оценка:** 2-3 часа

---

## 📅 Рекомендуемый порядок реализации

### Фаза 1: Batch операции (1-2 дня)
1. ✅ Batch-map для Button Matrix Items
2. ✅ Clone матрицы

### Фаза 2: Аналитика (2-3 дня)
3. ✅ Дополнительные эндпоинты аналитики
4. ✅ Эндпоинты для расходов
5. ✅ Система алертов

### Фаза 3: Улучшения (1-2 дня)
6. ✅ Клонирование рецептов
7. ✅ KPI Calculator

### Фаза 4: Опционально (по необходимости)
8. ⚪ Расширенный RBAC
9. ⚪ Документация API

---

## 📊 Итоговая оценка

| Приоритет | Задач | Время |
|-----------|-------|-------|
| 🔴 Высокий | 3 | 13-18 часов |
| 🟡 Средний | 3 | 9-12 часов |
| 🟢 Низкий | 2 | 10-13 часов |
| **ИТОГО** | **8** | **32-43 часа** |

---

## ✅ Критерии готовности

Проект считается полностью готовым, когда:
- ✅ Все batch операции реализованы
- ✅ Все эндпоинты из документации работают
- ✅ Система алертов функционирует
- ✅ Документация соответствует реализации

---

## 🔄 После реализации

1. Обновить `ARCHITECTURE.md` с новыми эндпоинтами
2. Добавить тесты для новых функций
3. Обновить `API_REFERENCE.md`
4. Создать релиз v1.2.0
