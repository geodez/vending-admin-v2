# Анализ данных для импорта рецептов

## 📋 Обзор файлов

### `product.json` (22 продукта)
Содержит информацию о продуктах (напитках) для вендингового аппарата:
- Цены продажи
- Видимость и включенность
- Порядок отображения
- Связь с рецептами через `recipeName`

### `recipe.json` (30 рецептов)
Содержит детальную информацию о рецептах приготовления:
- Шаги приготовления (`stepses`)
- Используемые канистры (`canisterIds`)
- Объемы воды и веса ингредиентов
- Параметры приготовления (espresso/instant атрибуты)

---

## 🔍 Ключевые находки

### 1. Canister IDs и их использование

В системе используются 5 типов канистр:
- **`0`** - Вода (используется в 5 шагах)
- **`1`** - Сироп (используется в 28 шагах) - возможно разные виды сиропов
- **`2`** - Молоко (используется в 25 шагах)
- **`3`** - Шоколад/какао (используется в 8 шагах)
- **`170`** - Кофе (используется в 24 шагах)

### 2. Соответствие Products ↔ Recipes

- **22 продукта** имеют `recipeName`
- **30 уникальных рецептов** в `recipe.json`
- **8 рецептов** не имеют соответствующих продуктов:
  - Американо 350 мл.
  - Американо Strong 250 мл.
  - Капучино Strong 250 мл.
  - Молочный коктейль клубника банан 250 мл.
  - Молочный коктейль с бананом 250 мл.
  - РАФ банановый 250 мл.
  - РАФ кокосовый 250 мл.
  - Флэт Уайт 250 мл.

### 3. Структура рецепта

Пример рецепта "Капучино 250 мл.":
```json
{
  "recipeName": "Капучино 250 мл.",
  "canisterIds": "2,170",
  "stepses": [
    {
      "canisterId": "2",      // Молоко
      "waterVolume": "85",     // 85 мл
      "gradientWeight": "25",  // 25 г
      "recipeOutOrder": "0"
    },
    {
      "canisterId": "170",     // Кофе
      "waterVolume": "75",     // 75 мл
      "gradientWeight": "12",  // 12 г
      "recipeOutOrder": "1"
    }
  ]
}
```

---

## 💡 Рекомендации по импорту

### ✅ Что БРАТЬ из данных

#### Из `product.json`:

1. **Для таблицы `drinks`:**
   - `recipeName` → `drinks.name` (уникальное название)
   - `visible && enable` → `drinks.is_active` (активен, если видим И включен)

2. **Для таблицы `machine_matrix` (опционально):**
   - `productId` → можно использовать как `machine_item_id` (если нужно)
   - `order` → порядок сортировки
   - `visible && enable` → `is_active`

3. **Дополнительные поля (если нужно расширить схему):**
   - `price` / `salePrice` → цена продажи (если добавить поле в `drinks`)
   - `makeTime` → время приготовления
   - `hotCold` → тип напитка (горячий/холодный)

#### Из `recipe.json`:

1. **Для таблицы `drinks`:**
   - `recipeName` → `drinks.name` (уникальное название)
   - Все рецепты → создавать записи в `drinks` (даже без продуктов)

2. **Для таблицы `drink_items`:**
   - `recipeName` → найти `drink_id` по `name`
   - `stepses[].canisterId` → маппинг на `ingredient_code`
   - `stepses[].gradientWeight` → `qty_per_unit` (в граммах)
   - `stepses[].waterVolume` → `qty_per_unit` (в миллилитрах) для воды/молока
   - Единица измерения: `'g'` для `gradientWeight`, `'ml'` для `waterVolume`

---

## ⚠️ Критические вопросы для решения

### 1. Маппинг Canister ID → Ingredient Code

**Требуется создать справочник маппинга:**

| Canister ID | Предполагаемый Ingredient Code | Название | Требует подтверждения |
|-------------|-------------------------------|----------|----------------------|
| `0` | `WATER` | Вода | ✅ |
| `1` | `SYRUP_*` | Сироп (возможно разные виды) | ⚠️ Нужно уточнить типы |
| `2` | `MILK` | Молоко | ✅ |
| `3` | `CHOCOLATE` или `COCOA` | Шоколад/какао | ⚠️ Нужно уточнить |
| `170` | `COFFEE_BEANS` | Кофе зерно | ✅ |

**Вопросы:**
- Существуют ли разные типы сиропов (canisterId=1)? Если да, как их различить?
- Какой именно ингредиент в canisterId=3 (шоколад или какао)?
- Есть ли другие canister IDs, которые не попали в эти файлы?

### 2. Обработка рецептов без продуктов

**8 рецептов не имеют соответствующих продуктов:**
- Создавать ли их в `drinks`?
- Если да, какой `is_active` ставить (по умолчанию `false`)?

### 3. Преобразование stepses → drink_items

**Логика преобразования:**

Для каждого шага в `stepses`:
- Если есть `gradientWeight` (вес в граммах) → использовать его как `qty_per_unit` с единицей `'g'`
- Если есть только `waterVolume` (объем в мл) → использовать его как `qty_per_unit` с единицей `'ml'`
- `canisterId` → найти соответствующий `ingredient_code` через справочник маппинга

**Пример:**
```
Рецепт "Капучино 250 мл.":
  Шаг 1: canisterId=2, gradientWeight=25г → drink_items: ingredient_code='MILK', qty_per_unit=25, unit='g'
  Шаг 2: canisterId=170, gradientWeight=12г → drink_items: ingredient_code='COFFEE_BEANS', qty_per_unit=12, unit='g'
```

**Вопрос:**
- Для canisterId=2 (молоко): использовать `gradientWeight` (г) или `waterVolume` (мл)? 
  - Судя по данным: `gradientWeight` указывает на вес молока в граммах
  - Но молоко обычно измеряется в миллилитрах
  - **Решено:** Молоко сухое, используем граммы (`g`). (Уточнено пользователем)

### 4. Дублирование рецептов

Некоторые рецепты имеют разные объемы (250 мл. и 350 мл.):
- "Капучино 250 мл." и "Капучино 350 мл."
- "Латте 250 мл." и "Латте 350 мл."
- и т.д.

**Вопрос:**
- Создавать отдельные записи в `drinks` для каждого объема?
- Или использовать одну запись с вариациями?

---

## 📊 План импорта (предварительный)

### Этап 1: Подготовка справочника маппинга
1. Создать таблицу или конфигурационный файл маппинга `canisterId → ingredient_code`
2. Уточнить единицы измерения для каждого типа канистры

### Этап 2: Импорт в `drinks`
1. Извлечь уникальные `recipeName` из обоих файлов
2. Создать записи в `drinks`:
   - `name` = `recipeName`
   - `is_active` = `true` если есть в `product.json` и `visible && enable`, иначе `false`

### Этап 3: Импорт в `drink_items`
1. Для каждого рецепта из `recipe.json`:
   - Найти `drink_id` по `recipeName`
   - Для каждого шага в `stepses`:
     - Определить `ingredient_code` через маппинг `canisterId`
     - Определить `qty_per_unit` и `unit` из `gradientWeight` или `waterVolume`
     - Создать запись в `drink_items`

### Этап 4: Импорт в `machine_matrix` (опционально)
1. Если нужно связать продукты с терминалами:
   - Использовать `productId` как `machine_item_id`
   - Найти `drink_id` по `recipeName`
   - Установить `is_active` из `visible && enable`

---

## ❓ Вопросы для уточнения

1. **Маппинг canisterId → ingredient_code:**
   - Подтвердите соответствие canister IDs ингредиентам
   - Есть ли разные типы сиропов (canisterId=1)?
   - Что именно в canisterId=3 (шоколад или какао)?

2. **Единицы измерения:**
   - Для молока (canisterId=2): использовать граммы (`gradientWeight`) или миллилитры (`waterVolume`)?
   - Для других ингредиентов: всегда граммы?

3. **Рецепты без продуктов:**
   - Создавать ли рецепты, которых нет в `product.json`?
   - Какой статус `is_active` для них?

4. **Дублирование объемов:**
   - Создавать отдельные записи для "Капучино 250 мл." и "Капучино 350 мл."?
   - Или использовать одну запись с вариациями?

5. **Дополнительные поля:**
   - Нужно ли хранить цену (`price`) в таблице `drinks`?
   - Нужно ли хранить время приготовления (`makeTime`)?
   - Нужно ли хранить тип напитка (`hotCold`)?

---

## 📝 Следующие шаги

1. ✅ **Завершено:** Анализ структуры данных
2. ⏳ **Ожидает решения:** Ответы на вопросы выше
3. ⏳ **Следующий этап:** Создание скрипта импорта после уточнения деталей
