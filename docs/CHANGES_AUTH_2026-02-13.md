# Изменения в системе авторизации - 13.02.2026

## Выполненные задачи

### 1. ✅ Исправление "Bot domain invalid"

**Проблема:** При попытке входа через Telegram Login Widget появлялась ошибка "Bot domain invalid".

**Решение:** Создана документация с инструкциями по настройке домена в BotFather.

**Файлы:**
- `docs/TELEGRAM_BOT_SETUP.md` - Пошаговая инструкция по настройке

**Действия для исправления:**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/setdomain`
3. Выберите бота `@coffeekznebot`
4. Укажите домен: `romanrazdobreev.store` (БЕЗ https://)

### 2. ✅ Добавлена авторизация по логину/паролю

**Реализовано:**
- Авторизация по email и паролю
- Хеширование паролей с использованием bcrypt
- Разделение прав доступа (owner/operator)
- Интерфейс с вкладками для выбора метода авторизации

**Изменения в backend:**

1. **Модель пользователя** (`app/models/user.py`):
   - Добавлены поля `email` и `hashed_password`
   - `telegram_user_id` теперь nullable

2. **Новые файлы:**
   - `app/auth/password.py` - Утилиты для работы с паролями
   - `backend/scripts/create_user.py` - Скрипт создания пользователей
   - `migrations/versions/0009_add_email_password_auth.py` - Миграция БД

3. **Обновленные файлы:**
   - `app/api/v1/auth.py` - Добавлен endpoint `/login`
   - `app/schemas/auth.py` - Добавлена схема `LoginRequest`
   - `app/crud/user.py` - Добавлена функция `get_user_by_email`
   - `requirements.txt` - Добавлена зависимость `passlib[bcrypt]`

**Изменения в frontend:**

1. **Обновленные файлы:**
   - `src/pages/LoginPage.tsx` - Добавлены вкладки Telegram/Email
   - `src/api/auth.ts` - Добавлен метод `loginWithPassword`
   - `src/types/api.ts` - Добавлен тип `LoginRequest`

2. **Интерфейс:**
   - Вкладка "🔐 Telegram" - авторизация через Telegram
   - Вкладка "📧 Email" - авторизация по email/паролю

## Разделение прав доступа

Система уже имела разделение прав, теперь оно работает для обоих методов авторизации:

### Owner (Владелец)
- Полный доступ ко всем разделам
- Доступ к шаблонам матриц
- Доступ к настройкам

### Operator (Оператор)
- Доступ к основным разделам (обзор, продажи, склад, рецепты, ингредиенты, расходы)
- НЕТ доступа к шаблонам матриц
- НЕТ доступа к настройкам

## Использование

### Применение миграции

```bash
# В Docker
docker-compose exec backend alembic upgrade head

# Локально
cd backend
alembic upgrade head
```

### Создание пользователя

```bash
cd backend
python scripts/create_user.py admin@example.com SecurePass123 owner "Администратор"
```

### Вход в систему

1. Откройте страницу входа
2. Выберите вкладку "📧 Email"
3. Введите email и пароль
4. Нажмите "Войти"

## API Endpoints

### POST /api/v1/auth/login
Авторизация по email/паролю

**Request:**
```json
{
  "email": "admin@example.com",
  "password": "password123"
}
```

**Response:**
```json
{
  "access_token": "eyJ...",
  "token_type": "bearer",
  "user": {
    "id": 1,
    "email": "admin@example.com",
    "telegram_user_id": null,
    "first_name": "Администратор",
    "role": "owner",
    "is_active": true
  }
}
```

## Безопасность

- ✅ Пароли хешируются с bcrypt
- ✅ JWT токены с истечением срока действия (7 дней)
- ✅ Валидация Telegram данных через HMAC-SHA256
- ✅ Whitelist пользователей (проверка в БД)
- ✅ Разделение прав доступа по ролям

## Документация

- `docs/AUTHENTICATION.md` - Полное руководство по авторизации
- `docs/TELEGRAM_BOT_SETUP.md` - Настройка Telegram бота
- `backend/scripts/create_user.py` - Скрипт создания пользователей

## Следующие шаги

1. **Настроить домен в BotFather** для исправления "Bot domain invalid"
2. **Применить миграцию** базы данных
3. **Создать пользователей** с email/паролем
4. **Протестировать** оба метода авторизации

## Примечания

- Оба метода авторизации работают параллельно
- Пользователь может иметь и Telegram ID, и email/пароль
- При создании через Telegram, email будет NULL
- При создании через email/пароль, telegram_user_id будет NULL
- Разделение прав работает одинаково для обоих методов
