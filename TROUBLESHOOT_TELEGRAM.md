# 🔍 Диагностика: Telegram Web App не работает

## 🚨 Симптомы и решения

### ❌ Симптом 1: "Веб-приложение не найдено"

**Описание:**
- Нажимаете "OPEN APP" в Telegram
- Telegram показывает ошибку: "Веб-приложение не найдено"

**Причины:**
1. Web App не настроен в @BotFather
2. URL неверный или недоступен
3. Используется HTTP вместо HTTPS

**Решение:**

✅ **Шаг 1:** Откройте [@BotFather](https://t.me/BotFather)

✅ **Шаг 2:** `/mybots` → выберите @coffeekznebot

✅ **Шаг 3:** `🌐 Web App Settings`

✅ **Шаг 4:** Введите URL: `https://admin.b2broundtable.ru`

✅ **Шаг 5:** Сохраните и подождите 5 минут

---

### ❌ Симптом 2: Web App открывается, но "Network Error"

**Описание:**
- Web App открывается
- Видите страницу входа
- При клике "Войти" → Network Error

**Причины:**
1. Backend не работает
2. API endpoint не доступен
3. TELEGRAM_BOT_TOKEN не совпадает

**Решение:**

✅ **Проверка 1:** Backend работает?

```bash
# На сервере
docker ps | grep vending-admin-backend

# Должны видеть контейнер в статусе "Up"
```

✅ **Проверка 2:** API доступен?

```bash
# Откройте в браузере
https://admin.b2broundtable.ru/api/health

# Должно вернуть: {"status":"healthy"}
```

✅ **Проверка 3:** TELEGRAM_BOT_TOKEN правильный?

```bash
# На сервере, откройте .env
cat /opt/vending-admin-v2/backend/.env | grep TELEGRAM_BOT_TOKEN

# Должно быть что-то вроде: TELEGRAM_BOT_TOKEN=123456789:ABCdef...
```

**Сравните с @BotFather:**
- Откройте @BotFather
- `/mybots` → выберите бота
- Посмотрите API Token
- Токены должны совпадать

✅ **Проверка 4:** Посмотрите логи backend

```bash
docker logs -f vending-admin-backend --tail 50

# Ищите строки:
# - "POST /v1/auth/telegram" - запрос прошёл
# - "User authenticated" - пользователь авторизован
# - "ValueError" или "Error" - проблема
```

---

### ❌ Симптом 3: Web App открывается, видю имя, но при клике "Войти" → "User not registered"

**Описание:**
- Web App открывается
- Показывается имя пользователя (например "Roman")
- При клике "Войти через Telegram" → "User not registered"

**Причина:**
- Пользователь с таким telegram_user_id не добавлен в базу данных

**Решение:**

✅ **Способ 1: Быстро через SQL**

```bash
# На сервере подключитесь к БД
docker exec -it vending-admin-postgres psql -U vending -d vending_admin

# Добавьте пользователя (узнайте свой telegram_user_id)
INSERT INTO users (telegram_user_id, username, role, is_active)
VALUES (602720033, 'roman', 'owner', true);

# Посмотрите добавленного пользователя
SELECT id, telegram_user_id, username, role FROM users;

# Выход
\q
```

✅ **Способ 2: Через админ-панель**

1. Откройте браузер
2. Перейдите на `https://admin.b2broundtable.ru/login?debug=true`
3. Нажмите "Войти" (используется test user 602720033)
4. Перейдите в Настройки → Пользователи
5. Добавьте нового пользователя с его telegram_user_id

✅ **Способ 3: Получить свой telegram_user_id**

Откройте в Telegram бота [@userinfobot](https://t.me/userinfobot) - он покажет ваш ID.

---

### ❌ Симптом 4: "Требуется Telegram" в браузере, но кнопка не открывает Telegram

**Описание:**
- Открыли `https://admin.b2broundtable.ru/login`
- Видите кнопку "Открыть в Telegram"
- Нажимаете - ничего не происходит или открывается неправильный URL

**Причина:**
- Браузер блокирует редирект на `t.me://`
- TELEGRAM_BOT_USERNAME неправильный

**Решение:**

✅ **Проверка 1:** Откройте консоль браузера (F12)

```javascript
// Скопируйте и выполните в консоли:
console.log('Telegram URL:', `https://t.me/coffeekznebot/vendingadmin`);

// Должна вывести URL
```

✅ **Проверка 2:** Откройте URL вручную в адресной строке

```
https://t.me/coffeekznebot/vendingadmin
```

Если это работает, то кнопка тоже должна работать.

✅ **Проверка 3:** Убедитесь что имя бота правильное

В `frontend/.env.production` должно быть:
```env
VITE_TELEGRAM_BOT_USERNAME=coffeekznebot
```

Если ваш бот имеет другое имя (например `@myadminbot`), измените на `myadminbot`.

---

### ❌ Симптом 5: Web App работает в Telegram но не работает в браузере с debug=true

**Описание:**
- `https://admin.b2broundtable.ru/login?debug=true`
- Видите "DEBUG MODE: Test данные загружены"
- При клике → Network Error

**Причина:**
- API не доступен
- Неправильный API_BASE_URL

**Решение:**

✅ **Проверка 1:** API работает?

```bash
curl -X POST https://admin.b2broundtable.ru/api/v1/auth/telegram \
  -H "Content-Type: application/json" \
  -d '{"init_data":"test"}'

# Должна быть ошибка валидации (это нормально),
# но не 404 или connection refused
```

✅ **Проверка 2:** Посмотрите консоль браузера (F12)

```
Network → XHR → Ищите POST /api/v1/auth/telegram
```

Посмотрите:
- Status: должен быть не 404
- URL: должен быть `https://admin.b2broundtable.ru/api/v1/auth/telegram`
- Response: может быть ошибка но не 404

✅ **Проверка 3:** Проверьте Nginx конфиг

```bash
# На сервере
nginx -t

# Должно быть: nginx: configuration file test is successful
```

---

## 🛠️ Чек-лист диагностики

Если Web App не работает, проверьте по порядку:

- [ ] Web App настроен в @BotFather (SETUP_TELEGRAM_BOT.md)
- [ ] URL в BotFather: `https://admin.b2broundtable.ru`
- [ ] HTTPS работает: `curl https://admin.b2broundtable.ru/login`
- [ ] API работает: `curl https://admin.b2broundtable.ru/api/health`
- [ ] TELEGRAM_BOT_TOKEN совпадает в .env и @BotFather
- [ ] Backend работает: `docker ps | grep backend`
- [ ] Пользователь в базе: `docker exec db psql ... SELECT * FROM users`
- [ ] Посмотрены логи backend: `docker logs backend`
- [ ] Посмотрена консоль браузера: F12 → Console
- [ ] Посмотрены Network запросы: F12 → Network

---

## 📱 Проверка по частям

### 1. Проверка Telegram

```bash
# Telegram доступен?
curl -s https://api.telegram.org/bot<TOKEN>/getMe | jq .

# Должен вернуть информацию о боте
```

### 2. Проверка Frontend

```bash
# Frontend доступен?
curl https://admin.b2broundtable.ru/login

# Должна быть HTML страница входа
```

### 3. Проверка API

```bash
# API доступен?
curl https://admin.b2broundtable.ru/api/health

# Должно вернуть: {"status":"healthy"}
```

### 4. Проверка Database

```bash
# Database работает?
docker exec vending-admin-postgres pg_isready

# Должно вернуть: accepting connections
```

### 5. Проверка авторизации

```bash
# Авторизация работает?
curl -X POST https://admin.b2broundtable.ru/api/v1/auth/telegram \
  -H "Content-Type: application/json" \
  -d '{
    "init_data":"query_id=test&user={\"id\":602720033}&auth_date=1&hash=test"
  }' | jq .

# Должно вернуть либо error валидации, но не 404
```

---

## 🆘 Если ничего не помогло

**Попробуйте перезагрузить всё:**

```bash
# На сервере
cd /opt/vending-admin-v2

# Остановите контейнеры
docker-compose -f backend/docker-compose.yml down

# Подождите 10 секунд
sleep 10

# Запустите заново
docker-compose -f backend/docker-compose.yml up -d

# Проверьте что всё запустилось
docker ps

# Посмотрите логи
docker-compose -f backend/docker-compose.yml logs -f
```

**После перезагрузки:**
1. Откройте @coffeekznebot в Telegram
2. Нажмите кнопку Web App
3. Попробуйте авторизоваться

---

## 💬 Получить помощь

Если ошибка не ясна, посмотрите:
1. Логи backend: `docker logs vending-admin-backend`
2. Консоль браузера: F12 → Console
3. Network запросы: F12 → Network
4. И скопируйте эту информацию в поиске решения
